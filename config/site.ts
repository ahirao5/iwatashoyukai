/**
 * ===================================================
 * 樟友会サイト設定ファイル
 * ===================================================
 * このファイルを編集するだけで、サイト全体の
 * 基本情報・連絡先・URLが一括で更新されます。
 *
 * ★ 公開前に必ず確認・更新してください ★
 * ===================================================
 */

export const siteConfig = {
  // --------------------------------------------------
  // 同窓会基本情報
  // --------------------------------------------------
  org: {
    name: "岩田中学校・高等学校同窓会 樟友会",
    nameShort: "樟友会",
    nameEn: "IWATA Alumni Association",
    foundedYear: 1990,
    president: "山田 信太郎",
    presidentNote: "四回生",
  },

  // --------------------------------------------------
  // 事務局連絡先
  // ★ 公開前に実際の情報に差し替えてください
  // --------------------------------------------------
  contact: {
    // 郵便番号（ハイフンなし 7桁）
    postalCode: "870-0000",        // ← 要変更

    // 住所（都道府県から）
    address: "大分県大分市○○町X-X-X 岩田中学校・高等学校内",  // ← 要変更

    // メールアドレス
    email: "info@shoyukai.iwata.ed.jp",   // ← 要変更

    // 電話番号（任意）
    tel: "",    // ← 設置する場合は記入

    // 問い合わせ受付時間のメモ
    officeHours: "平日 10:00〜16:00（学校行事等により不在の場合があります）",
  },

  // --------------------------------------------------
  // 本番サイトURL
  // ★ Vercel等でデプロイ後に実際のURLに変更してください
  // --------------------------------------------------
  url: "https://shoyukai.iwata.ed.jp",   // ← 要変更

  // --------------------------------------------------
  // 学校公式サイト・関連URL
  // --------------------------------------------------
  schoolUrl: "https://www.iwata.ed.jp",  // ← 要確認

  // --------------------------------------------------
  // 会費情報（会則第10条より）
  // --------------------------------------------------
  fee: {
    admission: 10000,       // 入会金（卒業時）
    firstTerm: 10000,       // 前期会費（卒業時）
    secondTerm: 10000,      // 後期会費（入会30年後）
    // 振込先口座情報 ★ 要変更
    bank: {
      name: "○○銀行",          // ← 要変更
      branch: "○○支店",        // ← 要変更
      type: "普通預金",
      number: "XXXXXXX",        // ← 要変更
      holder: "イワタチュウガッコウコウトウガッコウドウソウカイ ショウユウカイ",  // ← 要確認
    },
  },

  // --------------------------------------------------
  // SNS・外部リンク
  // ★ アカウントがあれば入力してください
  // --------------------------------------------------
  social: {
    x: "",          // ← 例: "https://twitter.com/shoyukai_iwata"
    facebook: "",   // ← 例: "https://www.facebook.com/shoyukai.iwata"
    instagram: "",  // ← 任意
  },

  // --------------------------------------------------
  // 名簿情報
  // --------------------------------------------------
  directory: {
    currentEdition: 7,
    publishedYear: 2024,
    priceNote: "有料（詳細は事務局へお問い合わせください）",
  },
} as const;

// 型エクスポート（他ファイルから参照する際に使用）
export type SiteConfig = typeof siteConfig;
